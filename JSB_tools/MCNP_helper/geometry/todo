Add functionality for groups of cells/surfaces. Allow for compliment on these (Easy to do on groups of cells)
Inhance like but to do things like inherit importance, materials, ect but for a entirely different geom.
Add a helper function to & all cells/surfs in a list
Add grouping functionality to tallies.
the get all cells thing is not very convientient. Use a groupo instead.
Think about how to make modularization of projects easier.
change e.g. RightCylinder to RightCylinderCell. Disable geometry setting in this case bc it doenst make senmse.
Padding cell geom to be more readabkle
Add a way to use the zo, dz paradime in cubiod surface
Allow the use of "pointers" via lists to allow parameter changes in real time of things like cubiod dims.